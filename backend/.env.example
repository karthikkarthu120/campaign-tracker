# Server Configuration
PORT=5000
NODE_ENV=development

# Database Configuration (Optional)
# Uncomment and configure these if you want to use MySQL
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=campaign_tracker

# Use in-memory storage instead of MySQL (recommended for development)
USE_IN_MEMORY=true

# Frontend URL for CORS (adjust if needed)
FRONTEND_URL=http://localhost:3000
